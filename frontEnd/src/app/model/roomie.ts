export interface Roomie {
    roomieid: number; // Matches 'RoomieId' in the backend model
    roomiename: string; // Matches 'RoomieName' in the backend model
    loginid: number; // Matches 'LoginId' in the backend model
  }  